/target
**/*.rs.bk
*.pdb

# Editors
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Local env / secrets
.env
.env.*
!.env.example

# aint-runtime's `db` module (milestone 25) - file-backed tables an
# AINT program creates at runtime, not source.
.aintdb/
