[package]
name = "aint-migrate"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Rewrites an AINT program to the latest idiomatic syntax (milestone 45), provably behavior-preserving: no AI, no network, no quota dependency."

[dependencies]
aint-ast = { path = "../ast" }

[dev-dependencies]
aint-parser = { path = "../parser" }
aint-fmt = { path = "../fmt" }
