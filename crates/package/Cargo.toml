[package]
name = "aint-package"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "AINT's package manifest, lockfile, and local dependency resolution (milestone 23)."

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
